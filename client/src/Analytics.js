import React, {Component} from 'react';
import {CLIENT_VERSION, REACT_VERSION, SERVER_URL} from './config';

export default class Analytics extends Component{
  render() {
    return (
        <div>
          You are on Analytics page.
        </div>
    );
  }
}
